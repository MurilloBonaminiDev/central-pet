# Optional override for local development tweaks.
# Usage: docker compose -f docker-compose.yml -f docker-compose.override.example.yml up
services:
  api:
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./backend:/app
