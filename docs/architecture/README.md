# Architecture docs

See [central-pet-arquitetura.md](./central-pet-arquitetura.md).

Stack note: the approved physical scaffold uses **FastAPI + SQLAlchemy + Alembic + JWT + Redis + Docker** for the backend, while preserving Clean Architecture / monorepo topology from the architecture document.
